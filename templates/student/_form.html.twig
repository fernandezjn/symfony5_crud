{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-sm mt-5 float-end"><i class="far fa-save"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
